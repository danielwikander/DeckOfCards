/**
 * Using an enum for the suits
 * (and Ranks, see Rank.java)
 * allows us to rank the suits after
 * their ordinal value in the enum.
 * eg. HEARTS = 1, DIAMOND = 2 etc.
 */
enum Suit {
   HEARTS,
   DIAMOND,
   CLUBS,
   SPADES
}
